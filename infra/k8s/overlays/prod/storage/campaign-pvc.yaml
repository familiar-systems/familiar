apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: campaign-pvc
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: ""
  volumeName: campaign-pv
  resources:
    requests:
      storage: 1Gi
