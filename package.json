{
  "name": "familiar-systems",
  "private": true,
  "scripts": {
    "lint": "pnpm -r run lint",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "typecheck": "tsc -b",
    "build:web": "pnpm --filter @familiar-systems/web build",
    "build:site": "pnpm --filter @familiar-systems/site build",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "oxfmt": "^0.33",
    "oxlint": "^1.48",
    "typescript": "^5.8",
    "vitest": "^3"
  }
}
