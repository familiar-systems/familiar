// The familiar.systems design system. Its product is CSS: import tokens + theme
// via the `@familiar-systems/design/styles.css` subpath from a Tailwind v4 entry
// stylesheet. This module is the JS entry, reserved for typed token constants if
// a consumer ever needs them in JS (e.g. motion durations for scripted animation).
export {};
