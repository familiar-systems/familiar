diff --git a/dist/client/index.cjs b/dist/client/index.cjs
index c2036e771e8ee9981ed5e8a9fd9f8834e98d0191..f6177e10bc691fe367be2dbd5fefc2df71d89d3c 100644
--- a/dist/client/index.cjs
+++ b/dist/client/index.cjs
@@ -77,7 +77,7 @@ var LoroWebsocketClient = class {
 		};
 		this.attachNetworkListeners();
 		this.ensureConnectedPromise();
-		this.connect();
+		this.connect().catch(() => {});
 	}
 	async resolveAuth(auth) {
 		if (typeof auth === "function") {
@@ -322,7 +322,7 @@ var LoroWebsocketClient = class {
 		const delay = immediate ? 0 : this.computeBackoffDelay(attempt);
 		this.reconnectTimer = setTimeout(() => {
 			this.reconnectTimer = void 0;
-			this.connect();
+			this.connect().catch(() => {});
 		}, delay);
 	}
 	clearReconnectTimer() {
@@ -1017,8 +1017,9 @@ var LoroWebsocketClient = class {
 	}
 	sendJoinPayload(payload) {
 		if (this.safeSend(this.ws, payload, "join")) return;
+		if (!this.shouldReconnect) return;
 		this.enqueueJoin(payload);
-		this.connect();
+		this.connect().catch(() => {});
 	}
 	flushQueuedJoins() {
 		if (!this.ws || this.ws.readyState !== WebSocket.OPEN) return;
diff --git a/dist/client/index.js b/dist/client/index.js
index b89e681952b7aebe9460eb6e893c3298d517a883..0d211866e267cd071f9b1b84dbbb5be272b57f38 100644
--- a/dist/client/index.js
+++ b/dist/client/index.js
@@ -79,7 +79,8 @@ var LoroWebsocketClient = class {
 		};
 		this.attachNetworkListeners();
 		this.ensureConnectedPromise();
-		this.connect();
+		// Fire-and-forget: swallow rejection so React strict-mode unmount → close() doesn't surface as Uncaught (in promise) Error: Disconnected.
+		this.connect().catch(() => {});
 	}
 	async resolveAuth(auth) {
 		if (typeof auth === "function") {
@@ -324,7 +325,8 @@ var LoroWebsocketClient = class {
 		const delay = immediate ? 0 : this.computeBackoffDelay(attempt);
 		this.reconnectTimer = setTimeout(() => {
 			this.reconnectTimer = void 0;
-			this.connect();
+			// Fire-and-forget: swallow rejection so React strict-mode unmount → close() doesn't surface as Uncaught (in promise) Error: Disconnected.
+			this.connect().catch(() => {});
 		}, delay);
 	}
 	clearReconnectTimer() {
@@ -1019,8 +1021,10 @@ var LoroWebsocketClient = class {
 	}
 	sendJoinPayload(payload) {
 		if (this.safeSend(this.ws, payload, "join")) return;
+		if (!this.shouldReconnect) return;
 		this.enqueueJoin(payload);
-		this.connect();
+		// Fire-and-forget: swallow rejection so React strict-mode unmount → close() doesn't surface as Uncaught (in promise) Error: Disconnected.
+		this.connect().catch(() => {});
 	}
 	flushQueuedJoins() {
 		if (!this.ws || this.ws.readyState !== WebSocket.OPEN) return;
